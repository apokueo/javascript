var data = [1,2,3,4,5], sum = 0

// compute the sum of the array of elements 

data.forEach(function(value {
    sum += value; 
}); 

sum 

// Now increment each array element 
data.forEach(function(v,i,a) {
    a[i] = v + 1; 
}); 

